extends SubViewportContainer

func _ready() -> void:
	pass
	#await RenderingServer.frame_post_draw
	#var img = get_viewport().get_texture().get_image()
	#img.save_png("user://frames/frame_%08d.png" % passo)
